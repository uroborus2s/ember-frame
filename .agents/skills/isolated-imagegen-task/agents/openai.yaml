interface:
  display_name: "Isolated Imagegen Task"
  short_description: "Run clean prompt-and-reference image tasks"
  default_prompt: "Use $isolated-imagegen-task to run a clean image generation task from only a prompt and attached reference images."
