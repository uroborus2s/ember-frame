# 音乐制作部工作流

```text
读取剧本和导演意图
  -> 分析情绪曲线
  -> 建立主题动机
  -> 规划每场 cue
  -> 查找/总结参考作品
  -> 输出 cue sheet
  -> 音乐 QC
  -> 剪辑交接
```

每个 cue 记录：

```text
music_cue_id
episode_id
scene_id
shot_range
start_trigger
end_trigger
emotion
instrumentation
tempo_feel
dialogue_safety
reference
```

