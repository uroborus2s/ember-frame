# 分集资产修复后全量审核

日期：2026-06-07

范围：`project/severed-homeland` 全局资产库、01 分集美术包、02 分集美术包、01/02 分集提示词包、资产清单与线程计划。

## 本轮修复

1. 修复 01 分集模板型角色误用单人身份锁的问题。
   - `E01_C016A`、`E01_C016B` 改为肃明基层虫吏模板继承锁。
   - `E01_C017` 改为混血奴兵模板继承锁。
   - `E01_C018` 改为纯虫族小兵模板继承锁。
   - `E01_C020` 改为北境攻城兵种群像模板继承锁。
   - `E01_C024A` 改为边墙军户模板继承锁。
   - `E01_C027` 改为残阳坳平民群像模板继承锁。
   - `E01_C023` 改为金河平民家庭群像模板继承锁。

2. 修复群像继承源。
   - `E01_C027` 不再把 `L001` 场景卡当作角色母卡；主继承源改为全局 `C025` 普通人族平民群像模板，并依赖 `K006` 与 `L001`。
   - `E01_C023` 不再继承 `C023` 逃难流民群像模板；主继承源改为全局 `C025` 普通人族平民群像模板，并依赖 `K006` 与 `L003`。
   - `E01_C024A` 保留为序幕状态卡，但明确它只从 `C024` 边墙普通军户群像模板派生，不成为新的具名角色母卡。

3. 修复 02 分集参考帧重名。
   - `E02_R003` 改名为“罗青禾药屋地窖 封门奴兵吹响气味牌参考画面”。
   - `E02_R007` 改名为“罗青禾药屋地窖 药粉反制奴兵参考画面”。
   - `E02_R019` 改名为“药屋火场 踢翻水缸与日月血牒参考画面”。
   - `E02_R023` 改名为“药屋火场 晏南枝看清伤势与日月血牒参考画面”。

## 全量审核结果

- JSON 语法：16 个核心 JSON 文件全部可解析。
- 重复 `asset_id`：01/02 分集资产索引、资产清单、提示词包均为 0。
- 重复 `output_path`：01/02 分集资产索引、资产清单、提示词包均为 0。
- 重复 `display_name`：01/02 分集资产索引、资产清单、提示词包均为 0。
- 根级全局资产泄漏：未发现活动中的 `e01`、`e02` 分集状态图留在根级 `assets/`。
- 01 分集提示词契约违规：0。
- 02 分集提示词契约违规：0。
- 模板继承锁审核：8 个 01 分集模板型角色/群像状态卡全部使用 `episode-template-inheritance-lock-v2-2026-06-07`，未再使用单人脸型/发型/体型身份硬锁。

## 群像资产结论

- 当前没有硬重复群像资产：没有重复 ID、重复输出路径或重复名称。
- `E01_C027` 可保留，前提是只作为第 01 集残阳坳山民被清查压迫的分集状态卡；它不替代全剧 `C025` 平民群像模板。
- `E01_C023` 可保留，前提是只作为第 01 集金河粮仓带家庭被征粮状态卡；它不继承逃难流民模板，也不替代全剧平民群像模板。
- `E01_C024A` 可保留为序幕年轻军户状态卡，但它只是 `C024` 边墙军户模板的单镜头状态派生，不应扩展为独立具名角色资产。

## 告警

- 01 分集有 65 个计划中或 blocked 状态的输出文件尚未生成。
- 02 分集有 63 个计划中或 blocked 状态的输出文件尚未生成。
- 这些缺失不属于继承或重复资产失败；它们表示后续图像生成队列尚未产出文件。若进入生成阶段，必须先使用本轮修复后的模板继承锁和参考图要求。
- 01 分集旧 B01 角色图与 B02 场景图已在当前 repair 状态中标记为 rejected/not-started，不再作为有效资产；后续应按修复后的继承锁重新生成。

## 最终状态

本轮修复后，角色一致性与场景一致性的继承关系已对齐：

- 具名角色继续使用身份继承硬锁。
- 官吏、奴兵、虫兵、军户、村民、家庭和兵种群像使用模板继承锁。
- 场景地理只作为场景或群像上下文锁，不再被误当作角色母卡。
- 分集群像资产只承担分集状态，不替代全剧主资产。
