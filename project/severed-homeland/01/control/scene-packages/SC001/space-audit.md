# SC001 四帧同空间审核

## 审核结论

状态：`blocked_pending_blender_exports`

坐标级审核通过，视觉证据审核未完成。`layout.yaml` 已把 R001/R002/R003/R004 锁定到同一个黑石城门、同一面外墙、同一座门楼和同一口骨钟；但由于本机没有可执行 Blender，`top-view.png`、`camera-map.png`、四张导演视角图、四张深度图和四张线稿图尚未从同一个 `.blend` 场景导出，因此不能给出最终视觉通过结论。

## 坐标级审核

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| R001/R002 同一外侧轴线 | 通过 | `r001` 摄像机在 `[0,-68,2.2]`，`r002` 摄像机在 `[0,-32,3.1]`，均看向同一城门。 |
| R002 比 R001 更近 | 通过 | `r002` 的 `y=-32` 明显比 `r001` 的 `y=-68` 更靠近门体 `y=-2.9`。 |
| R003 位于同一门楼内部 | 通过 | `r003` 摄像机在 `[-7.2,5.15,11.15]`，目标落在门楼左后侧军户/骨钟区域。 |
| R004 位于门楼后侧三分位 | 通过 | `r004` 摄像机在 `[-1.4,5.2,11.75]`，看向薛临墙、女墙和墙外方向。 |
| 同一口骨钟 | 通过 | 骨钟固定在 `[-4.8,5.7,13.2]`，R003/R004 均引用此位置。 |
| 兽族进攻方向 | 通过 | 攻击向量为 `[0,1,0]`，从 `y=-90` 压向同一城门；禁止 `y>0` 城内侧兽潮。 |
| 旗帜位置 | 通过 | 左右墙顶旗位固定在 `x=-18`、`x=18`，并锁定 P017 肃明旗。 |

## 未完成的视觉审核

以下审核必须在 Blender 导出后完成：

- 顶视图是否显示一墙一门和唯一攻城方向。
- 机位图是否显示 R001/R002 同轴推进、R003/R004 同属门楼空间。
- `r003_camera.png` 是否清楚呈现前景断矛、中景军户、后景骨钟三层关系。
- `r004_camera.png` 是否同时保留薛临墙、女墙、墙外方向和后景骨钟空间锚点。
- 深度图是否支持同一空间的前后层级，不出现遮挡逻辑反转。
- 线稿图是否足够清楚，可进入 ControlNet/线稿参考或手工修图流程。

## 返工规则

如果后续导出图显示空间错误，必须优先修改 `layout.yaml` 和 `build_sc001_blockout.py`，重新生成 `.blend`、顶视图、机位图、深度图和线稿图；不得只改 ComfyUI 提示词或局部修图。
