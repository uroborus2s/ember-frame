# SC001 四帧同空间审核

## 审核结论

状态：`ready_passed`

坐标级审核和视觉证据审核均通过。`layout.yaml` 把 R001/R002/R003/R004 锁定到同一个黑石城门、同一面外墙、同一座门楼和同一口骨钟；`blockout.blend`、顶视图、机位图、四张导演视角图、四张深度图和四张线稿图已由同一个 Blender 场景导出，可作为 SC001 四帧同空间证据。

## 坐标级审核

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| R001/R002 同一外侧轴线 | 通过 | `r001` 摄像机在 `[0,-68,2.2]`，`r002` 摄像机在 `[0,-32,3.1]`，均看向同一城门。 |
| R002 比 R001 更近 | 通过 | `r002` 的 `y=-32` 明显比 `r001` 的 `y=-68` 更靠近门体 `y=-2.9`。 |
| R003 位于同一门楼内部 | 通过 | `r003` 摄像机在 `[-7.2,5.15,11.15]`，目标落在门楼左后侧军户/骨钟区域。 |
| R004 位于门楼后侧三分位 | 通过 | `r004` 摄像机在 `[-1.4,5.2,11.75]`，看向薛临墙、女墙和墙外方向。 |
| 同一口骨钟 | 通过 | 骨钟固定在 `[-4.8,5.7,13.2]`，R003/R004 均引用此位置。 |
| 兽族进攻方向 | 通过 | 攻击向量为 `[0,1,0]`，从 `y=-90` 压向同一城门；禁止 `y>0` 城内侧兽潮。 |
| 旗帜位置 | 通过 | 左右墙顶旗位固定在 `x=-18`、`x=18`，并锁定 P017 肃明旗。 |

## 视觉证据审核

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 顶视图一墙一门 | 通过 | `top-view.png` 显示单一黑石外墙、单一城门、同一门楼和唯一兽族推进方向。 |
| 机位图同空间 | 通过 | `camera-map.png` 显示 R001/R002 在墙外同轴推进，R003/R004 落在同一门楼/墙顶空间。 |
| R001/R002 轴线 | 通过 | `shot-guides/r001_camera.png` 与 `shot-guides/r002_camera.png` 均从墙外看向同一门体，R002 更接近撞门点。 |
| R003 门楼层级 | 通过 | `shot-guides/r003_camera.png` 保留断矛、年轻军户、骨钟三层关系。 |
| R004 薛临墙站位 | 通过 | `shot-guides/r004_camera.png` 保留薛临墙、女墙、墙外方向和后景骨钟空间锚点。 |
| 深度图层级 | 通过 | `depth/r001_depth.png` 到 `depth/r004_depth.png` 与各自相机视角一致，未出现前后层级反转。 |
| 线稿可用性 | 通过 | `lineart/r001_lineart.png` 到 `lineart/r004_lineart.png` 能读出墙、门、门楼、骨钟、人物块和攻击方向。 |

## 生成交接结论

这四个分镜参考帧可以作为同一空间下的连续镜头交给 ComfyUI 或其他生图工具。当前限制不在空间证据，而在 ComfyUI 没有可用 checkpoint 和 ControlNet 模型；因此不得把关键帧候选图标记为已生成。

## 返工规则

如果后续导出图显示空间错误，必须优先修改 `layout.yaml` 和 `build_sc001_blockout.py`，重新生成 `.blend`、顶视图、机位图、深度图和线稿图；不得只改 ComfyUI 提示词或局部修图。
