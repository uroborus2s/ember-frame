# 第 20 章：读懂并修改 ComfyUI 工作流

> 建议时长：75-90 分钟
> 适用平台：macOS / Windows / Linux
> 本章目标：能拆解官方工作流并保存个人版本。

## 本章你会做成什么

| 产出 | 成功标准 |
| --- | --- |
| 主产出 | 个人 Wan2.2 基础工作流模板 |
| 操作记录 | 至少完成 2 组实例的输入、参数、截图和结果判断。 |
| 截图 | 保存到你的项目副本 `screenshots/`；课程示例图位于 `docs/assets/screenshots/chapter-20/`。 |
| 下一步输入 | 进入低显存优化时知道能改哪里 |

## 实操验证边界

本章随仓库提供工作流、界面截图和记录表。生成结果、耗时、显存峰值和质量评分必须由学习者在自己的 ComfyUI 环境中记录；凡未完成实测的位置，一律标为 `待实测`，不得写成已生成。

## 本章截图

### 复杂 FLF2V 工作流参考

![复杂 FLF2V 工作流参考](../assets/screenshots/chapter-20/20-01-wan22-14b-flf2v-template.webp)

用于练习分区阅读。

### 5B 简洁工作流参考

![5B 简洁工作流参考](../assets/screenshots/chapter-20/20-02-wan22-5b-ti2v-template.webp)

适合初学者先做个人模板。

## 90 分钟教学安排

| 环节 | 时间 | 做什么 |
| --- | ---: | --- |
| 成果预览 | 5 分钟 | 先看截图，明确本章最后要得到什么。 |
| 原理讲解 | 15 分钟 | 讲清 工作流修改 的变量和判断标准。 |
| 实例 A 跟做 | 20 分钟 | 用基础例跑通流程。 |
| 实例 B 对比 | 20 分钟 | 只改变一个变量，观察差异。 |
| 截图记录 | 10 分钟 | 保存节点、参数、目录或项目表截图。 |
| 审阅复盘 | 10-20 分钟 | 判断是否能进入下一章。 |

## 原理图

```mermaid
flowchart LR
  A["输入：官方工作流 JSON"] --> B["操作：分区阅读和轻量修改"]
  B --> C["变量：模型区/采样区/输出区"]
  C --> D["输出：个人模板"]
  D --> E["记录：截图/表格/文件名/复盘"]
```

## 显存档位建议

| 显存 | 推荐做法 | 风险控制 |
| ---: | --- | --- |
| 8GB | 只做 工作流修改 的低分辨率草稿：优先 5B、短帧数、单 seed。 | 不同时跑多个候选，不加载 14B 双阶段大模型。 |
| 12GB | 可完成 5B 完整练习；14B 只做小尺寸验证或等待 fp8/量化版本。 | 每次只改一个变量，失败先降帧数。 |
| 16GB | 可做 14B 小中尺寸流程，并保留草稿和精修两套参数。 | 先筛 seed，再提升分辨率。 |
| 24GB | 可做 工作流修改 的标准练习和 2-4 个候选对比。 | 仍要记录 seed、模型、steps、宽高、帧数、耗时。 |

## 本章使用的工作流或素材

- [本课程 Wan2.2 工作流包](../assets/workflows/wan22/)

## 跟做实操

1. 打开 ComfyUI 首页或本章项目记录表。
2. 加载本章推荐的 Wan2.2 工作流，或打开对应截图定位节点。
3. 填写实例 A 的提示词、素材或表格。
4. 保持其他参数不动，只改变本章指定变量。
5. 运行、截图或记录缺模型错误。
6. 填写实操记录表，写出可用性判断。

## 知识点：工作流分区

先找模型加载区、提示词区、采样区和输出区。

### 实例：模型加载区

| 项目 | 内容 |
| --- | --- |
| 输入 | 在 FLF2V 模板中找到 `UNETLoader`、`CLIPLoader`、`VAELoader` 节点。 |
| 操作 | 只测试变量 `分区=模型`，其他参数保持不变。 |
| 预期现象 | 能列出每个加载节点需要的模型文件名。 |
| 判断原则 | 模型加载区决定工作流能否启动。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`分区=模型`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：采样输出区

| 项目 | 内容 |
| --- | --- |
| 输入 | 在模板中找到 `KSampler` / `KSamplerAdvanced`、`VAEDecode`、`SaveVideo`。 |
| 操作 | 只测试变量 `分区=采样输出`，其他参数保持不变。 |
| 预期现象 | 能说明采样器负责生成，保存节点负责输出文件。 |
| 判断原则 | 采样输出区是参数和结果连接处。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`分区=采样输出`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 知识点：替换节点

只替换低风险节点，例如保存、预览、备注，不先改核心采样链。

### 实例：替换保存节点

| 项目 | 内容 |
| --- | --- |
| 输入 | 把输出文件名前缀从默认改为 `S01_SH01_test`。 |
| 操作 | 只测试变量 `替换=保存`，其他参数保持不变。 |
| 预期现象 | 生成成功后输出文件更容易管理。 |
| 判断原则 | 优先改保存前缀，不要先改核心模型链。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`替换=保存`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：替换预览节点

| 项目 | 内容 |
| --- | --- |
| 输入 | 保留保存节点，增加或定位预览节点用于检查中间结果。 |
| 操作 | 只测试变量 `替换=预览`，其他参数保持不变。 |
| 预期现象 | 不改变最终输出，只改善观察流程。 |
| 判断原则 | 预览节点是低风险修改。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`替换=预览`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 知识点：参数暴露

把常改参数放到显眼位置，减少误改核心节点。

### 实例：暴露 seed steps

| 项目 | 内容 |
| --- | --- |
| 输入 | 把 seed、steps、width、height、length 写到工作流旁的备注节点。 |
| 操作 | 只测试变量 `暴露=生成参数`，其他参数保持不变。 |
| 预期现象 | 学习者不用翻节点也能看到关键参数。 |
| 判断原则 | 参数暴露降低误操作。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`暴露=生成参数`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：暴露镜头编号

| 项目 | 内容 |
| --- | --- |
| 输入 | 在备注节点写 `Project=P01, Shot=S01_SH03, Version=v01`。 |
| 操作 | 只测试变量 `暴露=项目参数`，其他参数保持不变。 |
| 预期现象 | 输出和记录表能对上镜头编号。 |
| 判断原则 | 项目参数要和技术参数同等重要。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`暴露=项目参数`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 实操记录表

| 编号 | 输入素材/提示词 | 变量 | 模型/工作流 | seed | 参数 | 输出文件/表格 | 判断 |
| --- | --- | --- | --- | ---: | --- | --- | --- |
| A | 填实例 A | 只填一个核心变量 | 按本章推荐 | 固定或记录 | 草稿参数 | 运行后填写 | 可用/待修/淘汰 |
| B | 填实例 B | 与 A 形成对比 | 与 A 保持一致 | 固定或记录 | 不乱改 | 运行后填写 | 写清变化原因 |

## 截图清单

| 截图编号 | 文件 | 内容 | 状态 |
| --- | --- | --- | --- |
| 20-01 | `20-01-wan22-14b-flf2v-template.webp` | 复杂 FLF2V 工作流参考 | 已纳入本章 |
| 20-02 | `20-02-wan22-5b-ti2v-template.webp` | 5B 简洁工作流参考 | 已纳入本章 |

## 常见错误与排查

| 错误 | 常见原因 | 处理 |
| --- | --- | --- |
| 只写概念没有变量 | 不知道本章到底要验证什么。 | 每个实例只设置一个核心变量，并写入记录表。 |
| 结果失败但没有记录 | 缺少 seed、模型、提示词或截图。 | 先补记录，再决定是否重跑。 |
| 本机缺模型却写“已生成” | 伪造输出会破坏教程可信度。 | 记录缺失模型和预期输出，模型到位后再补生成截图。 |
| 参数一次改太多 | 无法判断变化来自哪里。 | 固定其他参数，只改本章变量。 |

## 本章验收清单

- [ ] 能说清 工作流修改 的核心变量。
- [ ] 完成两个实例的输入、参数、截图、结果判断和待实测记录。
- [ ] 至少保存 2 张截图。
- [ ] 如果生成失败，能说出失败是模型、显存、素材还是提示词问题。
- [ ] 写出下一章要继续使用的素材、参数或表格。

## 课后练习

1. 复制实例 A，换成你自己的项目主题。
2. 复制实例 B，只改一个变量，写出对比结论。
3. 补齐截图清单和实操记录表。
4. 写出本章最容易失败的一步，以及你的排查办法。


## 参考资料

- [ComfyUI Wan2.2 官方工作流教程](https://docs.comfy.org/tutorials/video/wan/wan2_2)
- [ComfyUI Wan2.2 示例](https://comfyanonymous.github.io/ComfyUI_examples/wan22/)
- [Wan2.2 官方仓库](https://github.com/Wan-Video/Wan2.2)
- [ComfyUI 系统需求](https://docs.comfy.org/installation/system_requirements/)

## 下一章衔接

第 21 章会做低显存和速度优化。
