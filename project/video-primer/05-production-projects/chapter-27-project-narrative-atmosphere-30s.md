# 第 27 章：项目二：30 秒叙事氛围短片

> 建议时长：90 分钟
> 适用平台：macOS / Windows / Linux
> 本章目标：完成 6 镜头叙事氛围短片流程。

## 本章你会做成什么

| 产出 | 成功标准 |
| --- | --- |
| 主产出 | 30 秒叙事短片粗剪方案 |
| 操作记录 | 至少完成 2 组实例的输入、参数、截图和结果判断。 |
| 截图 | 保存到你的项目副本 `screenshots/`；课程示例图位于 `docs/assets/screenshots/chapter-27/`。 |
| 下一步输入 | 进入音乐混剪前掌握连续性 |

## 实操验证边界

本章随仓库提供工作流、界面截图和记录表。生成结果、耗时、显存峰值和质量评分必须由学习者在自己的 ComfyUI 环境中记录；凡未完成实测的位置，一律标为 `待实测`，不得写成已生成。

## 本章截图

### 叙事 T2V 模板

![叙事 T2V 模板](../assets/screenshots/chapter-27/27-01-wan22-14b-t2v-template.webp)

环境和空镜可用 T2V。

### 叙事 I2V 模板

![叙事 I2V 模板](../assets/screenshots/chapter-27/27-02-wan22-14b-i2v-template.webp)

角色镜头用 I2V 保持身份。

## 90 分钟教学安排

| 环节 | 时间 | 做什么 |
| --- | ---: | --- |
| 成果预览 | 5 分钟 | 先看截图，明确本章最后要得到什么。 |
| 原理讲解 | 15 分钟 | 讲清 叙事短片项目 的变量和判断标准。 |
| 实例 A 跟做 | 20 分钟 | 用基础例跑通流程。 |
| 实例 B 对比 | 20 分钟 | 只改变一个变量，观察差异。 |
| 截图记录 | 10 分钟 | 保存节点、参数、目录或项目表截图。 |
| 审阅复盘 | 10-20 分钟 | 判断是否能进入下一章。 |

## 原理图

```mermaid
flowchart LR
  A["输入：故事梗概/角色卡"] --> B["操作：T2V/I2V 生成镜头"]
  B --> C["变量：故事/角色/情绪"]
  C --> D["输出：叙事粗剪"]
  D --> E["记录：截图/表格/文件名/复盘"]
```

## 显存档位建议

| 显存 | 推荐做法 | 风险控制 |
| ---: | --- | --- |
| 8GB | 只做 叙事短片项目 的低分辨率草稿：优先 5B、短帧数、单 seed。 | 不同时跑多个候选，不加载 14B 双阶段大模型。 |
| 12GB | 可完成 5B 完整练习；14B 只做小尺寸验证或等待 fp8/量化版本。 | 每次只改一个变量，失败先降帧数。 |
| 16GB | 可做 14B 小中尺寸流程，并保留草稿和精修两套参数。 | 先筛 seed，再提升分辨率。 |
| 24GB | 可做 叙事短片项目 的标准练习和 2-4 个候选对比。 | 仍要记录 seed、模型、steps、宽高、帧数、耗时。 |

## 本章使用的工作流或素材

- [本课程 Wan2.2 工作流包](../assets/workflows/wan22/)

## 跟做实操

1. 打开 ComfyUI 首页或本章项目记录表。
2. 加载本章推荐的 Wan2.2 工作流，或打开对应截图定位节点。
3. 填写实例 A 的提示词、素材或表格。
4. 保持其他参数不动，只改变本章指定变量。
5. 运行、截图或记录缺模型错误。
6. 填写实操记录表，写出可用性判断。

## 知识点：故事拆镜

叙事短片必须拆成信息明确的镜头。

### 实例：雨夜归家

| 项目 | 内容 |
| --- | --- |
| 输入 | 故事：一个人雨夜走回公寓；镜头包括街道远景、人物近景、门口停顿。 |
| 操作 | 只测试变量 `故事=归家`，其他参数保持不变。 |
| 预期现象 | 形成清晰叙事开端。 |
| 判断原则 | 归家主题用环境和动作推进。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`故事=归家`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：废墟发现光源

| 项目 | 内容 |
| --- | --- |
| 输入 | 故事：废墟中人物发现远处光源；镜头包括环境、发现、反应。 |
| 操作 | 只测试变量 `故事=发现`，其他参数保持不变。 |
| 预期现象 | 形成悬念和希望感。 |
| 判断原则 | 发现类故事需要反应镜头。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`故事=发现`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 知识点：人物连续性

角色一致性是叙事成立的基础。

### 实例：室内外同角色

| 项目 | 内容 |
| --- | --- |
| 输入 | 同一角色卡，镜头 A 在咖啡馆，镜头 B 在雨夜街道。 |
| 操作 | 只测试变量 `连续性=场景切换`，其他参数保持不变。 |
| 预期现象 | 角色应通过发型和服装保持一致。 |
| 判断原则 | 跨场景最容易丢身份。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`连续性=场景切换`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：远景到近景同角色

| 项目 | 内容 |
| --- | --- |
| 输入 | 先生成远景，再用同一参考图做近景反应。 |
| 操作 | 只测试变量 `连续性=景别切换`，其他参数保持不变。 |
| 预期现象 | 观众能认出同一人。 |
| 判断原则 | 景别变化也要保持身份字段。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`连续性=景别切换`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 知识点：情绪递进

镜头顺序要推动情绪变化。

### 实例：平静到紧张

| 项目 | 内容 |
| --- | --- |
| 输入 | 镜头 1 静坐，镜头 2 听到异响，镜头 3 快步离开。 |
| 操作 | 只测试变量 `情绪=紧张递进`，其他参数保持不变。 |
| 预期现象 | 情绪逐步升高。 |
| 判断原则 | 紧张感来自动作和节奏递进。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`情绪=紧张递进`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：孤独到希望

| 项目 | 内容 |
| --- | --- |
| 输入 | 镜头 1 空旷冷色，镜头 2 发现暖光，镜头 3 走向光源。 |
| 操作 | 只测试变量 `情绪=希望递进`，其他参数保持不变。 |
| 预期现象 | 从孤独转向希望。 |
| 判断原则 | 色彩可以承担情绪转折。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`情绪=希望递进`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 实操记录表

| 编号 | 输入素材/提示词 | 变量 | 模型/工作流 | seed | 参数 | 输出文件/表格 | 判断 |
| --- | --- | --- | --- | ---: | --- | --- | --- |
| A | 填实例 A | 只填一个核心变量 | 按本章推荐 | 固定或记录 | 草稿参数 | 运行后填写 | 可用/待修/淘汰 |
| B | 填实例 B | 与 A 形成对比 | 与 A 保持一致 | 固定或记录 | 不乱改 | 运行后填写 | 写清变化原因 |

## 截图清单

| 截图编号 | 文件 | 内容 | 状态 |
| --- | --- | --- | --- |
| 27-01 | `27-01-wan22-14b-t2v-template.webp` | 叙事 T2V 模板 | 已纳入本章 |
| 27-02 | `27-02-wan22-14b-i2v-template.webp` | 叙事 I2V 模板 | 已纳入本章 |

## 常见错误与排查

| 错误 | 常见原因 | 处理 |
| --- | --- | --- |
| 只写概念没有变量 | 不知道本章到底要验证什么。 | 每个实例只设置一个核心变量，并写入记录表。 |
| 结果失败但没有记录 | 缺少 seed、模型、提示词或截图。 | 先补记录，再决定是否重跑。 |
| 本机缺模型却写“已生成” | 伪造输出会破坏教程可信度。 | 记录缺失模型和预期输出，模型到位后再补生成截图。 |
| 参数一次改太多 | 无法判断变化来自哪里。 | 固定其他参数，只改本章变量。 |

## 本章验收清单

- [ ] 能说清 叙事短片项目 的核心变量。
- [ ] 完成两个实例的输入、参数、截图、结果判断和待实测记录。
- [ ] 至少保存 2 张截图。
- [ ] 如果生成失败，能说出失败是模型、显存、素材还是提示词问题。
- [ ] 写出下一章要继续使用的素材、参数或表格。

## 课后练习

1. 复制实例 A，换成你自己的项目主题。
2. 复制实例 B，只改一个变量，写出对比结论。
3. 补齐截图清单和实操记录表。
4. 写出本章最容易失败的一步，以及你的排查办法。


## 参考资料

- [ComfyUI Wan2.2 官方工作流教程](https://docs.comfy.org/tutorials/video/wan/wan2_2)
- [ComfyUI Wan2.2 示例](https://comfyanonymous.github.io/ComfyUI_examples/wan22/)
- [Wan2.2 官方仓库](https://github.com/Wan-Video/Wan2.2)
- [ComfyUI 系统需求](https://docs.comfy.org/installation/system_requirements/)

## 下一章衔接

第 28 章进入音乐/预告片混剪。
