# 第 28 章：项目三：音乐/预告片风格混剪

> 建议时长：90 分钟
> 适用平台：macOS / Windows / Linux
> 本章目标：把多个 AI 镜头按节奏组织成风格混剪。

## 本章你会做成什么

| 产出 | 成功标准 |
| --- | --- |
| 主产出 | 15-30 秒风格混剪方案 |
| 操作记录 | 至少完成 2 组实例的输入、参数、截图和结果判断。 |
| 截图 | 保存到你的项目副本 `screenshots/`；课程示例图位于 `docs/assets/screenshots/chapter-28/`。 |
| 下一步输入 | 进入毕业作品整理前掌握素材池复用 |

## 实操验证边界

本章随仓库提供工作流、界面截图和记录表。生成结果、耗时、显存峰值和质量评分必须由学习者在自己的 ComfyUI 环境中记录；凡未完成实测的位置，一律标为 `待实测`，不得写成已生成。

## 本章截图

### 预告片 T2V 镜头来源

![预告片 T2V 镜头来源](../assets/screenshots/chapter-28/28-01-wan22-14b-t2v-template.webp)

T2V 适合生成风格化环境和冲击镜头。

### FLF2V 转场镜头来源

![FLF2V 转场镜头来源](../assets/screenshots/chapter-28/28-02-wan22-14b-flf2v-template.webp)

FLF2V 可用于首尾帧控制的转场镜头。

## 90 分钟教学安排

| 环节 | 时间 | 做什么 |
| --- | ---: | --- |
| 成果预览 | 5 分钟 | 先看截图，明确本章最后要得到什么。 |
| 原理讲解 | 15 分钟 | 讲清 音乐混剪项目 的变量和判断标准。 |
| 实例 A 跟做 | 20 分钟 | 用基础例跑通流程。 |
| 实例 B 对比 | 20 分钟 | 只改变一个变量，观察差异。 |
| 截图记录 | 10 分钟 | 保存节点、参数、目录或项目表截图。 |
| 审阅复盘 | 10-20 分钟 | 判断是否能进入下一章。 |

## 原理图

```mermaid
flowchart LR
  A["输入：音乐节拍/镜头素材"] --> B["操作：按节拍安排镜头"]
  B --> C["变量：节奏/色彩/复用"]
  C --> D["输出：混剪时间线"]
  D --> E["记录：截图/表格/文件名/复盘"]
```

## 显存档位建议

| 显存 | 推荐做法 | 风险控制 |
| ---: | --- | --- |
| 8GB | 只做 音乐混剪项目 的低分辨率草稿：优先 5B、短帧数、单 seed。 | 不同时跑多个候选，不加载 14B 双阶段大模型。 |
| 12GB | 可完成 5B 完整练习；14B 只做小尺寸验证或等待 fp8/量化版本。 | 每次只改一个变量，失败先降帧数。 |
| 16GB | 可做 14B 小中尺寸流程，并保留草稿和精修两套参数。 | 先筛 seed，再提升分辨率。 |
| 24GB | 可做 音乐混剪项目 的标准练习和 2-4 个候选对比。 | 仍要记录 seed、模型、steps、宽高、帧数、耗时。 |

## 本章使用的工作流或素材

- [本课程 Wan2.2 工作流包](../assets/workflows/wan22/)

## 跟做实操

1. 打开 ComfyUI 首页或本章项目记录表。
2. 加载本章推荐的 Wan2.2 工作流，或打开对应截图定位节点。
3. 填写实例 A 的提示词、素材或表格。
4. 保持其他参数不动，只改变本章指定变量。
5. 运行、截图或记录缺模型错误。
6. 填写实操记录表，写出可用性判断。

## 知识点：节奏驱动镜头

音乐决定镜头长度和切点。

### 实例：电子音乐快切

| 项目 | 内容 |
| --- | --- |
| 输入 | 音乐 128 BPM；每 1-2 秒切一次，镜头选强运动和强光影。 |
| 操作 | 只测试变量 `节奏=快切`，其他参数保持不变。 |
| 预期现象 | 形成预告片式冲击感。 |
| 判断原则 | 快切需要短而明确的素材。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`节奏=快切`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：氛围音乐慢切

| 项目 | 内容 |
| --- | --- |
| 输入 | 音乐 70 BPM；每 4-6 秒切一次，镜头选慢推和环境空镜。 |
| 操作 | 只测试变量 `节奏=慢切`，其他参数保持不变。 |
| 预期现象 | 形成沉浸感。 |
| 判断原则 | 慢切需要稳定长镜头。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`节奏=慢切`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 知识点：视觉统一

混剪容易散，必须统一色彩和风格。

### 实例：冷色霓虹统一

| 项目 | 内容 |
| --- | --- |
| 输入 | 所有镜头加入 `cool cyan and blue neon palette`。 |
| 操作 | 只测试变量 `视觉=冷色`，其他参数保持不变。 |
| 预期现象 | 混剪视觉统一。 |
| 判断原则 | 统一色彩减少素材拼贴感。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`视觉=冷色`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：暖色胶片统一

| 项目 | 内容 |
| --- | --- |
| 输入 | 所有镜头加入 `warm amber film look, soft grain`。 |
| 操作 | 只测试变量 `视觉=暖色`，其他参数保持不变。 |
| 预期现象 | 画面更复古柔和。 |
| 判断原则 | 暖色适合回忆和人文感。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`视觉=暖色`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 知识点：素材复用

同一素材可以裁切、变速和重复使用。

### 实例：同镜头裁切复用

| 项目 | 内容 |
| --- | --- |
| 输入 | 同一广角镜头裁成 16:9、9:16 和细节近景。 |
| 操作 | 只测试变量 `复用=裁切`，其他参数保持不变。 |
| 预期现象 | 同素材服务多个平台。 |
| 判断原则 | 裁切复用要保护主体位置。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`复用=裁切`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


### 实例：同角色素材成预告片

| 项目 | 内容 |
| --- | --- |
| 输入 | 同一角色的 5 条候选镜头按情绪递进排序。 |
| 操作 | 只测试变量 `复用=角色素材`，其他参数保持不变。 |
| 预期现象 | 形成角色预告片段。 |
| 判断原则 | 素材复用要保持角色连续。 |

操作流程：

1. 打开本章对应的 Wan2.2 工作流或项目记录表。
2. 在记录表里写下变量：`复用=角色素材`。
3. 输入本例提示词、素材或镜头要求。
4. 按显存档位选择草稿参数；本机缺模型时记录缺失文件，不伪造输出。
5. 截图保存节点、参数或项目表，并写下本例是否可进入下一步。


## 实操记录表

| 编号 | 输入素材/提示词 | 变量 | 模型/工作流 | seed | 参数 | 输出文件/表格 | 判断 |
| --- | --- | --- | --- | ---: | --- | --- | --- |
| A | 填实例 A | 只填一个核心变量 | 按本章推荐 | 固定或记录 | 草稿参数 | 运行后填写 | 可用/待修/淘汰 |
| B | 填实例 B | 与 A 形成对比 | 与 A 保持一致 | 固定或记录 | 不乱改 | 运行后填写 | 写清变化原因 |

## 截图清单

| 截图编号 | 文件 | 内容 | 状态 |
| --- | --- | --- | --- |
| 28-01 | `28-01-wan22-14b-t2v-template.webp` | 预告片 T2V 镜头来源 | 已纳入本章 |
| 28-02 | `28-02-wan22-14b-flf2v-template.webp` | FLF2V 转场镜头来源 | 已纳入本章 |

## 常见错误与排查

| 错误 | 常见原因 | 处理 |
| --- | --- | --- |
| 只写概念没有变量 | 不知道本章到底要验证什么。 | 每个实例只设置一个核心变量，并写入记录表。 |
| 结果失败但没有记录 | 缺少 seed、模型、提示词或截图。 | 先补记录，再决定是否重跑。 |
| 本机缺模型却写“已生成” | 伪造输出会破坏教程可信度。 | 记录缺失模型和预期输出，模型到位后再补生成截图。 |
| 参数一次改太多 | 无法判断变化来自哪里。 | 固定其他参数，只改本章变量。 |

## 本章验收清单

- [ ] 能说清 音乐混剪项目 的核心变量。
- [ ] 完成两个实例的输入、参数、截图、结果判断和待实测记录。
- [ ] 至少保存 2 张截图。
- [ ] 如果生成失败，能说出失败是模型、显存、素材还是提示词问题。
- [ ] 写出下一章要继续使用的素材、参数或表格。

## 课后练习

1. 复制实例 A，换成你自己的项目主题。
2. 复制实例 B，只改一个变量，写出对比结论。
3. 补齐截图清单和实操记录表。
4. 写出本章最容易失败的一步，以及你的排查办法。


## 参考资料

- [ComfyUI Wan2.2 官方工作流教程](https://docs.comfy.org/tutorials/video/wan/wan2_2)
- [ComfyUI Wan2.2 示例](https://comfyanonymous.github.io/ComfyUI_examples/wan22/)
- [Wan2.2 官方仓库](https://github.com/Wan-Video/Wan2.2)
- [ComfyUI 系统需求](https://docs.comfy.org/installation/system_requirements/)

## 下一章衔接

第 29 章整理毕业作品流程包。
